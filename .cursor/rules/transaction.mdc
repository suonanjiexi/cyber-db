---
description: 
globs: 
alwaysApply: true
---
# 事务管理

Cyber-DB 的事务管理模块负责确保数据库操作的 ACID 特性（原子性、一致性、隔离性和持久性）。

## 核心功能

- 事务创建、提交和回滚
- 并发控制（MVCC - 多版本并发控制）
- 隔离级别支持
- 死锁检测和处理
- 事务超时管理
- 读写锁实现
- 故障恢复

## 隔离级别

支持以下隔离级别：
- 读未提交（Read Uncommitted）
- 读已提交（Read Committed）
- 可重复读（Repeatable Read）- 默认级别
- 串行化（Serializable）

## 事务实现

事务管理器实现了两阶段锁定协议（2PL）和多版本并发控制（MVCC）以确保事务的正确执行和数据一致性。在分布式环境中，还支持两阶段提交协议（2PC）和三阶段提交协议（3PC）。

